import PropTypes from 'prop-types';
const Recipe = ({recipie}) => {
    console.log(recipie)
    return (
        <div>
            
        </div>
    );
};
Recipe.propTypes={
    recipie:PropTypes.object
}
export default Recipe;